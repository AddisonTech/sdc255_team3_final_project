#include <stdio.h>
#include "calculations.h"

/*
    Calculation 1 Function
    Author: Adam Smithhisler

    Purpose:
    This function accepts two numbers, calculates their average,
    and returns the result.
*/

double calculationOne(double num1, double num2) {
    double result = (num1 + num2) / 2;
    return result;
}
